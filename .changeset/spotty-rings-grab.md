---
"granola-cli": minor
---

Add shell tab completion via `@bomb.sh/tab`. Supports bash, zsh, and fish.

```sh
# zsh
eval "$(granola complete zsh)"

# bash
source <(granola complete bash)

# fish
granola complete fish | source
```
